# Game preparation

These are the preparation scripts for the release. They check the
game version, patch a copy, and build the OnionOS folder
(without changing the original game files).

This directory is only a source mirror. Run `prepare.sh` from the downloaded
release, where the runtime and compiled tools are included.
